import React from 'react'
import ReactDOM from 'react-dom/client'
import WorkbookOrga from './WorkbookOrga.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <WorkbookOrga />
  </React.StrictMode>
)
